export default {
    // Bot permissions required
    requiredPermissions: [
        'ViewChannel',
        'SendMessages',
        'ManageMessages',
        'ManageRoles',
        'AddReactions'
    ],

    // Verification emoji
    verificationEmoji: '✅',

    // Logging
    enableLogging: true
};
